
export const INITIAL_POINTS = 0; // Start with 0 points
export const POINTS_PER_CLICK = 1; // Base points earned per click

export const ITEM_DROP_CHANCE_PER_SECOND = 0.10; // 10% chance per second for passive item drop
export const ITEM_DROP_CHANCE_PER_CLICK = 0.02; // 2% chance per click for item drop

export const ARTIFICE_DROP_CHANCE_PER_SECOND = 0.02; // 2% chance per second, Artifices are rarer
export const ARTIFICE_DROP_CHANCE_PER_CLICK = 0.005; // 0.5% chance per click for artifice drop
